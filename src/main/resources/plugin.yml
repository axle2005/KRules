name: ${name}
main: ${package}.${name}
version: ${version}
author: KaiNoMood

commands:
   krules:
      description: Manage
   acceptrules:
      description: Accept rules
   rules:
   	  description: read rules

permissions:
    krules.manage:
        description: Manage
        default: op
